import { PageContainer } from "../../../styles/page.style";

/**
 * 스마트 미세먼지 데이터 현황 page 컴포넌트
 * @returns {JSX.Element}
 */
export const SmartFineDustDataStatusPage = (): JSX.Element => {
    return <PageContainer>💨 스마트 미세먼지 데이터 현황 page 컴포넌트 📊</PageContainer>;
};
