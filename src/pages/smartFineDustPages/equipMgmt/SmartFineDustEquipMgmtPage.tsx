import { PageContainer } from "../../../styles/page.style";

/**
 * 스마트 미세먼지 장비 관리 page 컴포넌트
 * @returns {JSX.Element}
 */
export const SmartFineDustEquipMgmtPage = (): JSX.Element => {
    return <PageContainer>💨 스마트 미세먼지 장비 관리 page 컴포넌트 ⚙️</PageContainer>;
};
