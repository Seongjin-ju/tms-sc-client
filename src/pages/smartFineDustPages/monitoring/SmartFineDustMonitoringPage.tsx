import { PageContainer } from "../../../styles/page.style";

/**
 * 스마트 미세먼지 모니터링 page 컴포넌트
 * @returns {JSX.Element}
 */
export const SmartFineDustMonitoringPage = (): JSX.Element => {
    return <PageContainer>💨 스마트 미세먼지 모니터링 page 컴포넌트 🖥️</PageContainer>;
};
